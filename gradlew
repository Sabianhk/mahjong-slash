#!/bin/sh

# Gradle wrapper start-up script.
# This is a minimal wrapper — Android Studio will auto-download the full wrapper JAR.

echo "ERROR: gradle-wrapper.jar not found."
echo "Open this project in Android Studio — it will download the Gradle wrapper automatically."
echo "Or run: gradle wrapper --gradle-version=8.11.1"
exit 1
